@page "/login"

<h3>Login</h3>
@if (string.IsNullOrEmpty(Message) == false)
{
    <div class="alert alert-danger">
        @Message
    </div>
}

<EditForm Model="@LoginVM" OnValidSubmit="HandleLogin">
    <DataAnnotationsValidator />
    <ValidationSummary />

    <MudGrid>
        <MudItem xs="12">
            <div class="d-flex justify-center">
                <MudIcon Ic Color="Color.Primary" Size="Size.Large" Style="width: 100px; height: 100px;" />
            </div>
        </MudItem>
        <MudItem xs="12">
            <MudText Typo="Typo.h4">Login</MudText>
        </MudItem>
        <MudItem xs="12">
            <MudTextField T="string" Label="Email" Variant="Variant.Outlined"
                          @bind-Value="LoginVM.Email" For="@(() => LoginVM.Email)" />
        </MudItem>
        <MudItem xs="12">
            <MudTextField Label="Password" Variant="Variant.Outlined"
                          @bind-Value="LoginVM.Password" For="@(() => LoginVM.Password)"
                          InputType="InputType.Password" Adornment="Adornment.End" AdornmentIcon="@LoginVM.Password"  />
        </MudItem>
        <MudItem xs="12" Class="d-flex justify-center">
            <MudButton ButtonType="ButtonType.Submit" Variant="Variant.Filled" Color="Color.Primary" Size="Size.Large" Style="width: 100%;">Login</MudButton>
        </MudItem>
    </MudGrid>
</EditForm>

